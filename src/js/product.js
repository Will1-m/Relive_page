document.addEventListener('DOMContentLoaded',async()=>{const root=document.getElementById('producto-detalle');if(!root)return;const id=new URLSearchParams(location.search).get('producto');try{const p=await Catalog.getProduct(id);if(!p)throw Error('not found');root.innerHTML='<div class="row g-4"><div class="col-md-6"><img class="img-fluid rounded product-detail-image" src="'+(p.imagen_url||p.imagen||'assets/placeholder.svg')+'" alt="'+p.nombre+'"></div><div class="col-md-6"><div class="small text-muted">'+(p.marca||'')+'</div><h1 class="h2">'+p.nombre+'</h1><div class="h4 text-success">$'+Number(p.precio||0).toFixed(2)+'</div><p>'+(p.descripcion||'')+'</p><p class="small text-muted">'+(p.codigo?'Código: '+p.codigo:'Código no disponible')+'</p><button class="btn btn-dark add-product">Añadir al carrito</button></div></div>';root.querySelector('.add-product').onclick=()=>{Cart.add(p.id);root.querySelector('.add-product').textContent='Añadido al carrito'}}catch(e){root.innerHTML='<p class="text-danger">No se pudo cargar el producto.</p>';console.error(e)}});